<?php

namespace ClickNow\Checker\Config;

use Mockery as m;

/**
 * @group config/compiler
 * @covers \ClickNow\Checker\Config\Compiler\TaskCompilerPass
 */
class TaskCompilerPassTest extends \PHPUnit_Framework_TestCase
{
    public function testA()
    {
        /*$m = \Mockery::mock(ContainerBuilder::class);
        $m->shouldReceive('getParameter')->with('tasks')->once()->andReturn(['aa' => []]);
        $m->shouldReceive('findTaggedServiceIds')->with(TaskCompilerPass::TAG_TASK)->once()->andReturn([
        'a' => [['config' => 'a']]
        ]);

        $a = new TaskCompilerPas*/

    }
}
